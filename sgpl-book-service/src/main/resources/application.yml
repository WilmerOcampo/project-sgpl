spring:
  application:
    name: book-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/book_db
    username: root
    password: Por100pr3
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8081
